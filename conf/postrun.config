// Post-run configuration for SQS integration
// This config is used by main-sqs.nf's workflow.onComplete handler

params {
    // SQS Configuration
    sqs_queue_url = 'https://sqs.us-east-1.amazonaws.com/850787717197/sales-prod-PackagerQueue-2BfTcvCBFuJA'
    sqs_region = 'us-east-1'

    // WRROC Configuration
    wrroc_max_wait_seconds = 60
    wrroc_check_interval_ms = 500
}
